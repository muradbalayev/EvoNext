/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.unsplash.com',
        'histudy.pixcelsthemes.com'
      ],
    },
  }
  
  module.exports = nextConfig
  